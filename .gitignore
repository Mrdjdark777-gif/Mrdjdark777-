# Приватный SSH-ключ доступа к Oracle Cloud серверу — секрет, никогда не коммитить.
*.key

# Локальные артефакты Playwright MCP (снапшоты страниц со smoke-тестов).
.playwright-mcp/

# Байткод hook-скриптов Claude Code (создаётся при каждом их запуске).
.claude/hooks/__pycache__/
